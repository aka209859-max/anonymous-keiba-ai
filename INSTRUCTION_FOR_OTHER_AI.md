# 🎯 他AI調査依頼用 - 完全指示文書

## 📌 依頼の背景

地方競馬予測システム「Anonymous Keiba AI」のPhase 4において、2026年1月のシミュレーション実行時にデータベーススキーマ不整合エラーが発生しています。Windows環境での実行結果とGitHubリポジトリの完全調査に基づき、根本原因を特定し修正方法を提案してください。

---

## 🔗 重要リンク

- **GitHubリポジトリ**: https://github.com/aka209859-max/anonymous-keiba-ai
- **ブランチ**: phase4_specialized_models
- **プルリクエスト**: https://github.com/aka209859-max/anonymous-keiba-ai/pull/3
- **最新コミット**: 7253aed (2026-02-04)

---

## 🚨 現在発生中のエラー

### メインエラー: `column s.seibetsu does not exist`

```
ERROR: column s.seibetsu does not exist
LINE 69: s.seibetsu,
         ^
HINT: Perhaps you meant to reference the column "s.seibetsu_code".
```

**発生場所**: `simulate_2026_hitrate_only.py` の69行目

**実行環境**: Windows 11, PostgreSQL 5432, データベース: pckeiba

---

## 📁 調査対象ファイル

### 1. 問題のスクリプト
**ファイル**: `simulate_2026_hitrate_only.py`
- **GitHub URL**: https://github.com/aka209859-max/anonymous-keiba-ai/blob/phase4_specialized_models/simulate_2026_hitrate_only.py
- **機能**: 2026年1月の実データで的中率シミュレーション
- **使用テーブル**: `nvd_se` (LEFT JOIN `nvd_ra`)
- **エラー行**: 69行目 `s.seibetsu,`

### 2. Phase 3学習スクリプト（参照用）
**ファイル**: `extract_training_data_v2.py`
- **GitHub URL**: https://github.com/aka209859-max/anonymous-keiba-ai/blob/phase4_specialized_models/extract_training_data_v2.py
- **機能**: Phase 3モデル学習時のデータ抽出
- **使用カラム**: 95行目、202行目、268行目で `se.seibetsu_code` を使用

---

## 🗄️ データベース情報

### 接続情報
```
host: 127.0.0.1
port: 5432
database: pckeiba
user: postgres
password: postgres123
```

### 主要テーブル

#### nvd_se (出走馬情報)
- レース識別: `kaisai_nen`, `kaisai_tsukihi`, `keibajo_code`, `race_bango`
- 馬情報: `umaban`, `ketto_toroku_bango`, `kakutei_chakujun`
- **問題のカラム**: `seibetsu` vs `seibetsu_code` （どちらが正しい?）
- その他: `barei`, `kishu_code`, `chokyoshi_code`, `futan_juryo`
- 前走データ: `prev1_rank`, `prev1_time`, `prev1_last3f`, ... (前5走分)

#### nvd_ra (レース情報)
- レース識別: `kaisai_nen`, `kaisai_tsukihi`, `keibajo_code`, `race_bango`
- レース詳細: `kyori`, `track_code`, `tenko_code`, `grade_code`
- **確認済み**: `shusso_tosu` (出走頭数)
- **確認済み**: `babajotai_code_shiba`, `babajotai_code_dirt` (馬場状態)

---

## ✅ 既に修正済みの問題

### 修正1: 出走頭数 (コミット 8f918fb)
```diff
- s.shusso_tosu,
+ r.shusso_tosu,
```
**理由**: `shusso_tosu` は `nvd_ra` テーブルに存在

### 修正2: 馬場状態 (コミット a963ca9)
```diff
- r.baba_jotai_code,
+ r.babajotai_code_shiba,
+ r.babajotai_code_dirt,
```
**理由**: 芝とダートで別カラムに分かれている

### 修正3: 対象期間 (コミット cc91feb)
```diff
- kaisai_tsukihi >= '0101' AND kaisai_tsukihi <= '0203'
+ kaisai_tsukihi >= '0101' AND kaisai_tsukihi <= '0131'
```
**理由**: 2月のデータが不完全なため1月のみに限定

---

## ❓ 調査してほしい項目

### 🔴 最優先: seibetsu カラム名の確定

**調査内容**:
1. `nvd_se` テーブルの実際のカラム名は？
   - [ ] `seibetsu` が存在するか？
   - [ ] `seibetsu_code` が存在するか？
   - [ ] 別のカラム名か？

2. Phase 3学習スクリプトとの整合性確認
   - `extract_training_data_v2.py` では `se.seibetsu_code` を使用（95, 202, 268行目）
   - なぜシミュレーションスクリプトでは `s.seibetsu` なのか？

3. 正しい修正方法
   - `s.seibetsu` → `s.seibetsu_code` に修正すべきか？
   - 他に見落としているカラムはないか？

### 🟡 追加調査項目

#### 1. データベーススキーマの完全確認

以下のSQLを実行して、実際のカラム名を確認:
```sql
-- nvd_se テーブルのカラム一覧
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'nvd_se'
ORDER BY ordinal_position;

-- nvd_ra テーブルのカラム一覧
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'nvd_ra'
ORDER BY ordinal_position;
```

#### 2. カラム名の一貫性チェック

以下のカラムが正しいか確認:
- [ ] `nvd_se.seibetsu` vs `nvd_se.seibetsu_code` ← **最優先**
- [ ] `nvd_se.barei` (馬齢)
- [ ] `nvd_se.kishu_code` (騎手コード)
- [ ] `nvd_se.chokyoshi_code` (調教師コード)
- [ ] `nvd_se.futan_juryo` (負担重量)
- [ ] `nvd_se.ketto_toroku_bango` (血統登録番号)
- [ ] `nvd_se.kakutei_chakujun` (確定着順)
- [ ] `nvd_ra.kyori` (距離)
- [ ] `nvd_ra.track_code` (トラックコード)

#### 3. 前走データカラムの存在確認

以下のカラムが `nvd_se` に存在するか:
- [ ] `prev1_rank`, `prev1_time`, `prev1_last3f`, `prev1_weight`
- [ ] `prev1_corner1`, `prev1_corner2`, `prev1_corner3`, `prev1_corner4`
- [ ] `prev1_kyori`, `prev1_track`, `prev1_baba`
- [ ] `prev2_*`, `prev3_*`, `prev4_*`, `prev5_*` (同様の構造)

#### 4. 2026年1月データの存在確認

```sql
SELECT 
    keibajo_code,
    COUNT(*) as data_count
FROM nvd_se
WHERE kaisai_nen = '2026'
  AND kaisai_tsukihi >= '0101'
  AND kaisai_tsukihi <= '0131'
  AND kakutei_chakujun IS NOT NULL
  AND kakutei_chakujun != '0'
GROUP BY keibajo_code
ORDER BY keibajo_code;
```

**期待される結果**: 10競馬場で合計約9,922件

---

## 📋 調査手順（推奨）

### Step 1: GitHubリポジトリのクローン
```bash
git clone https://github.com/aka209859-max/anonymous-keiba-ai.git
cd anonymous-keiba-ai
git checkout phase4_specialized_models
```

### Step 2: 関連ファイルの確認
```bash
# シミュレーションスクリプト
cat simulate_2026_hitrate_only.py

# Phase 3学習スクリプト（参照）
cat extract_training_data_v2.py

# カラム名の比較
grep -n "seibetsu" simulate_2026_hitrate_only.py
grep -n "seibetsu" extract_training_data_v2.py
```

### Step 3: データベーススキーマの確認
```bash
# スキーマ確認スクリプトを実行（Windows環境で）
python check_db_schema.py
```

または直接SQLで:
```sql
-- psql接続
psql -h 127.0.0.1 -p 5432 -U postgres -d pckeiba

-- nvd_se テーブル構造
\d nvd_se

-- nvd_ra テーブル構造
\d nvd_ra
```

### Step 4: 2026年データの確認
```bash
python check_date_range.py
```

### Step 5: 修正案の作成
- 特定した不一致をすべてリストアップ
- 修正版SQLクエリを提案

---

## 📊 期待される調査結果

### 最低限必要な情報

1. **seibetsu カラムの正しい名前**
   - `nvd_se.seibetsu` は存在するか？
   - `nvd_se.seibetsu_code` が正しいか？

2. **修正内容の確定**
   - 69行目: `s.seibetsu` → `s.seibetsu_code` に修正すべきか？

3. **その他の不整合の有無**
   - 他にも修正が必要なカラムはあるか？

### 理想的な調査結果

4. **完全なカラム名リスト**
   - `nvd_se` テーブルの全カラム（特に重要なもの）
   - `nvd_ra` テーブルの全カラム（特に重要なもの）

5. **修正版SQLクエリ**
   - `simulate_2026_hitrate_only.py` の48-151行目の修正版

6. **データ存在確認**
   - 10競馬場それぞれの2026年1月データ件数

---

## 🎯 最終目標

この調査結果を元に、以下を実現する:

### 1. エラーなしでの実行
- すべてのSQLエラーを解消
- 10競馬場すべてでデータ抽出成功

### 2. 的中率の計算
- 全体的中率
- 印別的中率（◎本命、○対抗、▲単穴、△連下、×その他）
- 競馬場別的中率

### 3. 出力ファイルの生成
- `simulation_2026_hitrate_results.csv`: 全予測結果（約9,922件）
- `simulation_2026_hitrate_summary.csv`: サマリー
- `simulation_2026_hitrate_summary.txt`: テキストレポート

### 4. 最終レポート作成
- Note用Markdown
- X (Twitter) 用280字以内
- Discord用Embed形式

---

## 📝 調査結果の報告形式

### 1. カラム名の修正リスト
```markdown
【必須修正】
- 行69: s.seibetsu → s.seibetsu_code
- 行XX: [その他の修正があれば]

【確認済み（修正不要）】
- 行62: r.shusso_tosu （修正済み）
- 行65-66: r.babajotai_code_shiba, r.babajotai_code_dirt （修正済み）
```

### 2. 修正版SQLクエリ
```sql
-- simulate_2026_hitrate_only.py の48-151行目の修正版
SELECT 
    -- レース識別情報
    s.kaisai_nen,
    s.kaisai_tsukihi,
    ...
    -- 修正箇所
    s.seibetsu_code,  -- ← 修正: s.seibetsu から変更
    s.barei,
    ...
FROM nvd_se s
LEFT JOIN nvd_ra r ON ...
WHERE ...
```

### 3. データ存在確認
```
【2026年1月データ件数】
- 大井(44): XXX件
- 船橋(43): XXX件
- 川崎(45): XXX件
...
合計: 約9,922件
```

### 4. その他の発見事項
- 潜在的な問題の指摘
- 推奨事項
- 注意点

---

## 🔧 利用可能な確認スクリプト

リポジトリに以下のスクリプトが含まれています:

1. **check_db_schema.py**
   - `nvd_se` と `nvd_ra` のカラム名を確認
   - 重要カラムをフィルタして表示

2. **check_date_range.py**
   - 2026年1-2月のデータ件数を競馬場別に確認
   - 日付範囲別のデータ分布を表示

3. **simulate_2026_hitrate_only.py**
   - シミュレーション実行スクリプト（修正対象）

4. **extract_training_data_v2.py**
   - Phase 3学習時のデータ抽出（参照用）

---

## 📚 参考ドキュメント

### リポジトリ内のドキュメント
- `AI_INVESTIGATION_INSTRUCTION.md`: 完全調査指示書（詳細版）
- `CURRENT_STATUS_SUMMARY.md`: 現状サマリー
- `README.md`: プロジェクト全体の説明
- `session_logs/`: 過去のセッションログ
- `training_reports/`: Phase 3学習レポート

### プルリクエスト
- **PR #3**: https://github.com/aka209859-max/anonymous-keiba-ai/pull/3
- タイトル: 「fix: 2026年1月シミュレーション実行のためのスキーマ修正とドキュメント追加」
- 13コミット分の修正内容を含む

---

## ⚠️ 重要な注意点

1. **ハルシネーション厳禁**
   - GitHubリポジトリの実際のコードを確認してください
   - データベーススキーマの推測は避け、実際のテーブル構造を確認してください

2. **Phase 3との整合性**
   - Phase 3学習時 (`extract_training_data_v2.py`) と同じカラム名を使用すべき
   - 学習データと予測データでスキーマ不一致があると予測精度が落ちる

3. **Windows環境の制約**
   - データベースは Windows 11 のローカル環境（127.0.0.1:5432）
   - PC-KEIBA 付属の PostgreSQL

4. **データの完全性**
   - 2026年2月のデータは不完全（879件のみ）
   - 2026年1月のデータのみを使用（約9,922件）

---

## 📞 質問・確認事項

調査中に不明点があれば、以下を確認してください:

1. **データベースアクセス**
   - PostgreSQL に接続可能か？
   - `pckeiba` データベースが存在するか？

2. **Python環境**
   - `psycopg2`, `pandas`, `numpy`, `lightgbm` がインストールされているか？

3. **Git/GitHub**
   - リポジトリにアクセス可能か？
   - `phase4_specialized_models` ブランチをチェックアウトできるか？

4. **データの存在**
   - 2026年1月のデータは実際に存在するか？
   - 10競馬場すべてでデータがあるか？

---

## ✅ 調査完了のチェックリスト

- [ ] `nvd_se.seibetsu` vs `nvd_se.seibetsu_code` の正しいカラム名を特定
- [ ] `nvd_ra` テーブルの馬場状態カラム名を確認（修正が正しいか検証）
- [ ] `nvd_se` と `nvd_ra` の重要カラムの完全なリストを取得
- [ ] 前走データカラム（`prev1_*` ～ `prev5_*`）の存在を確認
- [ ] 2026年1月の実データ件数を競馬場別に確認
- [ ] 修正版SQLクエリを提案
- [ ] その他の潜在的な問題を指摘
- [ ] 修正内容を `simulate_2026_hitrate_only.py` に適用
- [ ] シミュレーション実行の成功を確認

---

## 🎯 この指示文書の使い方

1. **他AIへの依頼時**
   - この文書全体をコピーして依頼
   - 「ハルシネーションなしで、GitHubリポジトリとデータベースを実際に確認してください」と明示

2. **調査の進め方**
   - Step 1から順番に実施
   - 各項目のチェックリストを埋めながら進める

3. **結果の報告**
   - 報告形式セクションに従って結果をまとめる
   - 修正版SQLクエリを必ず提供

---

**この調査指示書を使って、他のAIに完全な調査を依頼してください！**

最終更新: 2026-02-04
GitHub: https://github.com/aka209859-max/anonymous-keiba-ai
PR: https://github.com/aka209859-max/anonymous-keiba-ai/pull/3
