# 🏆 anonymous競馬AIシステム「最高峰への進化」ロードマップ

## 📊 現在の完成度: 76% → 目標: 100%

---

## エグゼクティブサマリー

本ロードマップは、現在76%の完成度にある**anonymous地方競馬AI予想システム**を、予測精度と投資収益率（ROI）の両面において**「最高峰」**の水準へ引き上げるための戦略的計画書である。

### 現状の評価
- ✅ **Phase 0-6**: 完全実装済み（データ取得→特徴量作成→予測→アンサンブル→配信）
- ✅ **基本機能**: Learning to Rank、馬体重・天候・騎手・コース・距離データ統合
- ❌ **不足機能**: Boruta特徴量選択、Optuna自動チューニング、期待値ベース購入戦略、オッズ統合

### 目指すゴール
単なる「予測機」から、市場の歪みを検知し利益を生み出す**「定量的取引エンジン（Quantitative Trading Engine）」**へ進化する。

---

## 🎯 Phase 7-10: 最高峰への進化計画

| Phase | 機能名 | 目的 | 完成度向上 | 優先度 |
|-------|--------|------|-----------|--------|
| **Phase 7** | Advanced Feature Selection | ノイズ除去・過学習防止 | 76% → 85% | 🔴 HIGH |
| **Phase 8** | Auto-Optimization | ハイパーパラメータ最適化 | 85% → 92% | 🔴 HIGH |
| **Phase 9** | Betting Engine | 期待値最大化・資金管理 | 92% → 100% | 🔴 HIGH |
| **Phase 10** | Simulation & Backtest | ROI検証・継続改善 | 100% (運用) | 🟡 MEDIUM |

---

## Phase 7: Advanced Feature Selection（高度な特徴量選択）

### 目的
地方競馬特有の**ノイズ（競走中止・失格・降着）**を排除し、**真に有意な特徴量のみ**を抽出する。

### 実装内容

#### 1. Greedy Borutaの導入
```python
# 従来のBorutaの5〜40倍高速
# alpha=0.10で競馬データのノイズに対応
from greedyboruta import GreedyBorutaPy
from lightgbm import LGBMClassifier

feat_selector = GreedyBorutaPy(
    estimator=LGBMClassifier(
        n_estimators=500,
        class_weight='balanced',
        importance_type='gain'
    ),
    n_estimators='auto',
    perc=100,
    alpha=0.10,  # 有意水準を緩和
    max_iter=200,
    verbose=2
)
```

#### 2. ノイズフィルタリング
- **競走除外データの完全排除**: 「取消」「中止」「除外」「失格」を行単位で削除
- **降着処理**: 「4(3)」→ 確定着順「4」を採用（馬券的中が目的）
- **欠損値処理**: LightGBMのネイティブ欠損処理を活用（`use_missing=True`）

#### 3. パラメータ戦略
| パラメータ | デフォルト | 地方競馬推奨値 | 理由 |
|-----------|----------|--------------|------|
| `alpha` | 0.05 | 0.10 | ノイズの多いドメインに対応 |
| `max_iter` | 100 | 200-300 | 十分な試行回数を確保 |
| `two_step` | True | True | 多重検定対策 |

### 期待される効果
- ✅ **過学習の抑制**: 偶発的パターンを排除
- ✅ **計算効率の向上**: 特徴量削減により後続処理が高速化
- ✅ **予測精度（AUC）の改善**: +0.01〜0.03の向上

### 実装ファイル
```
scripts/phase7_feature_selection/
├── greedy_boruta_selector.py    # Greedy Boruta実装
├── noise_filter.py               # ノイズフィルタリング
└── run_feature_selection.py     # 実行スクリプト
```

---

## Phase 8: Auto-Optimization（自動最適化）

### 目的
**Optuna 3.x系**を用いた段階的チューニングにより、LightGBMのポテンシャルを極限まで引き出す。

### 実装内容

#### 1. Optuna LightGBM Tunerの統合
```python
import optuna.integration.lightgbm as lgb_optuna

# 段階的最適化（Stepwise Tuning）
tuner = lgb_optuna.LightGBMTunerCV(
    fixed_params,
    dtrain,
    folds=KFold(n_splits=5),
    time_budget=7200,  # 2時間
    optuna_seed=42
)
tuner.run()
```

#### 2. 最適化パラメータ範囲

| パラメータ | 役割 | 探索範囲 | 地方競馬への適用理由 |
|-----------|------|----------|-------------------|
| `num_leaves` | モデルの複雑さ | 31〜127 | データ量が少ないため過学習防止 |
| `min_child_samples` | 過学習防止 | 20〜100 | 異常値を無視させる |
| `lambda_l1/l2` | 正則化 | 1e-8〜10.0 (Log) | ノイズ対策で強力な正則化 |
| `feature_fraction` | 多様性確保 | 0.4〜1.0 | 決定木の多様性を確保 |
| `scale_pos_weight` | クラス不均衡対策 | 1.0〜sqrt(不均衡比) | 確率較正を維持 |

#### 3. クラス不均衡対策

**❌ 避けるべき**: `is_unbalance=True` → 予測確率が極端に歪む

**✅ 推奨**: `scale_pos_weight` を探索対象に含める
```python
balance_ratio = neg_count / pos_count
fixed_params['scale_pos_weight'] = balance_ratio ** 0.5
```

### 期待される効果
- ✅ **AUCスコアの大幅改善**: +0.02〜0.05
- ✅ **ロバスト性の向上**: 未知データへの汎化性能向上
- ✅ **自動化による運用効率化**: 手動調整の完全排除

### 実装ファイル
```
scripts/phase8_auto_optimization/
├── optuna_tuner.py              # Optunaチューニング
├── best_params.json             # 最適パラメータ保存
└── run_optimization.py          # 実行スクリプト
```

---

## Phase 9: Betting Engine（ベッティングエンジン）

### 目的
**予測確率**を**期待値**に変換し、**Kelly基準**による資金管理を実装する。

### 実装内容

#### 1. Harvilleの公式による3連単確率計算
```python
def calculate_harville_probabilities(win_probs: dict) -> dict:
    """
    Harvilleの公式: P(i,j,k) = p_i × (p_j/(1-p_i)) × (p_k/(1-p_i-p_j))
    """
    trifecta_probs = {}
    for h1, h2, h3 in permutations(horses, 3):
        p1, p2, p3 = win_probs[h1], win_probs[h2], win_probs[h3]
        prob = p1 * (p2 / (1-p1)) * (p3 / (1-p1-p2))
        trifecta_probs[(h1, h2, h3)] = prob
    return trifecta_probs
```

#### 2. Kelly基準による資金配分
```python
def calculate_kelly_stakes(prob, odds, bankroll, fractional=0.25):
    """
    Kelly Formula: f* = (bp - q) / b
    Fractional Kelly: bet = bankroll × f* × 0.25
    """
    b = odds - 1.0  # ネットオッズ
    f_star = (prob * odds - 1) / b
    if f_star > 0:
        f_adj = f_star * fractional  # 1/4 Kelly
        amount = int(bankroll * min(f_adj, 0.05))  # 上限5%
        return amount
    return 0
```

#### 3. 期待値フィルタリング
```python
EV = prob × odds - 1

# 正の期待値を持つ馬券のみ購入
positive_ev_bets = bets_df[bets_df['EV'] > 0]
```

### 戦略の選択肢

| 戦略 | 特徴 | 適用馬券 |
|------|------|---------|
| **高適合率（High Precision）** | 的中率重視、低配当でも確実 | 複勝・馬連 |
| **高再現率（High Recall）** | 穴馬も拾う、高配当狙い | 3連複・3連単 |
| **Kelly最適化** | 自動バランス調整 | 全馬券種 |

### 期待される効果
- ✅ **回収率の最大化**: 的中率ではなくROIを最適化
- ✅ **リスク管理**: 破産確率の制御
- ✅ **馬単・3連複の精度改善**: ユーザー報告の課題を解決

### 実装ファイル
```
scripts/phase9_betting_engine/
├── harville_calculator.py       # Harville公式
├── kelly_optimizer.py           # Kelly基準
├── expected_value_filter.py     # EV計算
└── run_betting_engine.py        # 実行スクリプト
```

---

## Phase 10: Simulation & Backtest（シミュレーション・バックテスト）

### 目的
過去の確定オッズデータを用いて、**Phase 7-9の統合システム**のROIを検証する。

### 実装内容

#### 1. バックテスト環境構築
```python
def backtest_betting_strategy(
    predictions_df,      # Phase 5のアンサンブル予測
    actual_results_df,   # 実際の着順
    odds_df,             # 確定オッズ
    initial_bankroll=100000
):
    """
    - 各レースでKelly基準によるベット額を計算
    - 的中/外れを判定し、資金の推移を記録
    - ROI、最大ドローダウン、シャープレシオを算出
    """
    # 実装略
```

#### 2. 評価指標
| 指標 | 計算式 | 目標値 |
|------|--------|--------|
| **ROI** | (総回収額 - 総投資額) / 総投資額 | > 10% |
| **的中率** | 的中数 / 総投票数 | > 30% |
| **最大ドローダウン** | 最大資金減少率 | < 30% |
| **シャープレシオ** | (平均利益 - 無リスク利子率) / 標準偏差 | > 1.0 |

#### 3. PDCAサイクル
1. **Plan**: Phase 7-9のパラメータを設定
2. **Do**: バックテストを実行
3. **Check**: ROIが目標値に達しているか確認
4. **Act**: パラメータを調整し再実行

### 実装ファイル
```
scripts/phase10_backtest/
├── backtest_engine.py           # バックテスト実行
├── performance_metrics.py       # 評価指標計算
└── run_backtest.py              # 実行スクリプト
```

---

## 📅 実装スケジュール

| Phase | 実装期間 | 工数（人日） | 依存関係 |
|-------|---------|------------|---------|
| Phase 7 | 1週間 | 3日 | Phase 0-1完了 |
| Phase 8 | 1週間 | 4日 | Phase 7完了 |
| Phase 9 | 2週間 | 5日 | Phase 3-5完了 |
| Phase 10 | 1週間 | 3日 | Phase 7-9完了 |
| **合計** | **5週間** | **15日** | - |

---

## 🎯 完成後のシステム構成

```
anonymous-keiba-ai/
├── scripts/
│   ├── phase0_data_acquisition/       [✅ 完成]
│   ├── phase1_feature_engineering/    [✅ 完成]
│   ├── phase3_binary/                 [✅ 完成]
│   ├── phase4_ranking/                [✅ 完成]
│   ├── phase4_regression/             [✅ 完成]
│   ├── phase5_ensemble/               [✅ 完成]
│   ├── phase6_betting/                [✅ 完成]
│   ├── phase7_feature_selection/      [🔴 NEW]
│   ├── phase8_auto_optimization/      [🔴 NEW]
│   ├── phase9_betting_engine/         [🔴 NEW]
│   └── phase10_backtest/              [🔴 NEW]
├── models/
│   ├── binary/                        [Phase 3用]
│   ├── ranking/                       [Phase 4-1用]
│   ├── regression/                    [Phase 4-2用]
│   └── best_params.json               [🔴 Phase 8で生成]
├── docs/
│   ├── ROADMAP_TO_EXCELLENCE.md       [🔴 本ドキュメント]
│   ├── TECHNICAL_SPEC.md              [🔴 技術仕様書]
│   └── phase3_completion_report.md    [✅ 既存]
└── README.md                          [✅ 更新予定]
```

---

## 🔧 技術スタック（Phase 7-10追加分）

| 機能 | ライブラリ | バージョン | 用途 |
|------|-----------|----------|------|
| 特徴量選択 | `greedyboruta` | latest | Greedy Boruta実装 |
| 自動最適化 | `optuna` | 3.x | ハイパーパラメータ探索 |
| 資金管理 | `numpy`, `pandas` | latest | Kelly基準計算 |
| バックテスト | `matplotlib` | latest | 結果可視化 |

### インストールコマンド
```bash
pip install greedyboruta optuna matplotlib
```

---

## 📈 期待される成果

### 完成度の推移
```
Phase 0-6完成: 76%
↓
Phase 7完成: 85% (+9%)
↓
Phase 8完成: 92% (+7%)
↓
Phase 9完成: 100% (+8%)
```

### 予測精度の向上
| 指標 | 現在 | Phase 7 | Phase 8 | 目標 |
|------|------|---------|---------|------|
| **AUC** | 0.77 | 0.78 | 0.82 | 0.85 |
| **的中率** | 20% | 25% | 30% | 35% |
| **回収率** | 60% | 80% | 100% | 120% |

---

## 🎓 重要な設計思想

### 1. 「予測機」から「取引エンジン」へ
- **従来**: 着順予測のみ
- **進化後**: 期待値計算 + 資金管理

### 2. ノイズとの戦い
- **Greedy Boruta**: シャドウ特徴量で真のシグナルを抽出
- **Kelly基準**: 過信を防ぐFractional Kelly（1/4 Kelly）

### 3. 数学的根拠の重視
- **Harvilleの公式**: 3連単確率の理論値
- **Kelly基準**: 対数成長率の最大化

---

## 🚀 次のアクション

### 開発者向け
1. **Phase 7の実装開始**: `greedyboruta`のインストールとテスト
2. **ノイズフィルタリングロジックの実装**: 競走除外データの除外
3. **Phase 1との統合**: 特徴量作成後にBoruta選択を挿入

### システム管理者向け
1. **実行環境の準備**: Python 3.14、必要ライブラリのインストール
2. **データベースの整備**: 過去オッズデータの収集（Phase 10用）
3. **バックアップの実施**: 既存モデルの保存

---

## 📚 参考文献

1. **Greedy Boruta論文**: "GreedyBorutaPy: A faster and more efficient feature selection algorithm" (2024)
2. **Optuna公式ドキュメント**: https://optuna.readthedocs.io/
3. **Harvilleの公式**: Harville, D. A. (1973). "Assigning probabilities to the outcomes of multi-entry competitions"
4. **Kelly基準**: Kelly, J. L. (1956). "A New Interpretation of Information Rate"
5. **PC-KEIBA標準仕様書**: 本プロジェクト内部ドキュメント

---

## 📝 更新履歴

| 日付 | バージョン | 更新内容 |
|------|----------|---------|
| 2026-02-09 | v1.0 | 初版作成（Phase 7-10のロードマップ策定） |

---

## 💬 お問い合わせ

本ロードマップに関する質問・提案は、GitHubのIssueまたはプロジェクトチャネルまでお願いします。

**最高峰の地方競馬AI予想システムの完成を目指して！** 🏆
