# JRDB データ仕様書（AI予測システム用）

**作成日**: 2026-02-14  
**対象**: anonymous-keiba-ai-jra プロジェクト Phase 0 データ取得  
**参照**: JRDB公式仕様書、DBman レイアウト定義

---

## 📋 目次

1. [概要](#概要)
2. [主要ファイル種別一覧](#主要ファイル種別一覧)
3. [ファイル別詳細仕様](#ファイル別詳細仕様)
4. [Python実装サンプル](#python実装サンプル)
5. [JRA-VANとの統合戦略](#jra-vanとの統合戦略)

---

## 概要

JRDBが提供するデータは、**固定長テキストファイル**形式で配信され、専用ダウンローダー「Ikkatsu」または「GetJRDB」を使用して取得する。全ファイルはLZH圧縮されており、Shift-JIS (cp932) でエンコードされている。

### データ特性

- **配信形式**: LZH圧縮アーカイブ
- **エンコーディング**: Shift-JIS (cp932)
- **改行コード**: CRLF (`\r\n`)
- **フォーマット**: 固定長（各項目のバイト位置が厳密に決定）
- **ファイル命名規則**: `[種別][YYMMDD].txt` (例: `SED241231.txt`)
- **欠損値**: スペース埋め（数値項目は'0'埋め）

### AI予測に必須のファイル種別

| 優先度 | ファイル種別 | 名称 | 用途 |
|--------|------------|------|------|
| ★★★ | `SED` | 成績データ | 過去の走破タイム、着順、IDM、ペース指数等 |
| ★★★ | `KYI` | 騎手・調教師・厩舎データ | 調教指数、厩舎指数、パドックコメント |
| ★★☆ | `BAC` | 馬場データ | 馬場差、高速化指数 |
| ★★☆ | `CYB` | 前日情報 | 前日IDM、前日印、前日オッズ |
| ★☆☆ | `CHA` | 調教データ | 追い切りタイム、調教コース |
| ★☆☆ | `SKB` | 成績拡張データ | 外厩情報、放牧先、休養理由 |
| ★☆☆ | `TYB` | 当日情報 | 馬体診断、パドック所見 |
| ★☆☆ | `UKC` | 馬基本データ | 馬の生年月日、性別、生産者 |

---

## 主要ファイル種別一覧

### 1. SED（成績データ）

**ファイル名**: `SEDyymmdd.txt` (例: `SED241231.txt`)  
**レコード長**: 約1400バイト  
**1レコード**: 1頭の1レース分の成績データ

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー（年月日+場所+R） | `24123105` = 2024年12月31日東京5R |
| 8 | 2 | horse_no | int | 馬番 | `05` |
| 10 | 10 | horse_id | str | JRDB馬ID（内部管理用） | `2019001234` |
| 20 | 4 | idm | int | IDM（スピード指数） | `0068` = 68 |
| 24 | 3 | pace_idx | int | ペース指数 | `042` = 42 |
| 27 | 3 | track_idx | int | 馬場指数（当日の馬場状態補正） | `050` = 50 |
| 30 | 3 | up_idx | int | 上がり指数 | `038` = 38 |
| 33 | 3 | position_idx | int | 位置取り指数 | `045` = 45 |
| 36 | 3 | tenkai_idx | int | ペース予想指数 | `040` = 40 |
| 39 | 1 | tenkai_mark | str | 展開記号 | `A`-`H`（Aが最も有利な展開） |
| 40 | 2 | distance_suitability | int | 距離適性 | `80` = 80% |
| 42 | 2 | surface_suitability | int | 芝ダ適性 | `85` = 85% |
| 44 | 2 | class_code | int | クラス | `05`=未勝利, `10`=1勝, `15`=2勝, `20`=3勝, `30`=オープン, `40`=G3, `50`=G2, `60`=G1 |
| 46 | 2 | finish_order | int | 着順 | `01` = 1着 |
| 48 | 4 | finish_time_sec | float | 走破タイム（秒、0.1単位） | `0914` = 91.4秒 |
| 52 | 3 | last_3f_time | float | 上がり3F（秒、0.1単位） | `332` = 33.2秒 |
| 55 | 2 | corner1_position | int | コーナー通過順1 | `05` |
| 57 | 2 | corner2_position | int | コーナー通過順2 | `04` |
| 59 | 2 | corner3_position | int | コーナー通過順3 | `03` |
| 61 | 2 | corner4_position | int | コーナー通過順4 | `02` |
| 63 | 4 | horse_weight | int | 馬体重 | `0480` = 480kg |
| 67 | 3 | horse_weight_diff | int | 馬体重増減 | `+12` または `-08` |
| 70 | 3 | jockey_idx | int | 騎手指数 | `055` = 55 |
| 73 | 3 | trainer_idx | int | 調教師指数 | `048` = 48 |
| 76 | 3 | stable_idx | int | 厩舎指数 | `052` = 52 |

#### IDMの補正内容

IDMは以下の要素を補正した総合スピード指数：

1. **馬場差補正**: 開催日ごとの馬場の高速化・タフさを標準化
2. **ペース補正**: スローペース/ハイペースによるタイムの歪みを平準化
3. **不利補正**: レース中の出遅れ、不利、進路妨害等を加味
4. **展開補正**: 逃げ馬有利/差し馬有利などの展開バイアスを除去

→ **異なる競馬場、異なる日付のレースでも比較可能な絶対値指標**

---

### 2. KYI（騎手・調教師・厩舎データ）

**ファイル名**: `KYIyymmdd.txt`  
**レコード長**: 約1800バイト  
**1レコード**: 1頭の1レース分の人的情報

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー | `24123105` |
| 8 | 2 | horse_no | int | 馬番 | `05` |
| 10 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 20 | 5 | jockey_code | str | 騎手コード | `01001` |
| 25 | 12 | jockey_name | str | 騎手名（全角） | `福永祐一` |
| 37 | 3 | jockey_idx | int | 騎手指数 | `055` |
| 40 | 3 | jockey_expectancy_idx | int | 騎手期待値指数（勝率×連対率） | `048` |
| 43 | 5 | trainer_code | str | 調教師コード | `01010` |
| 48 | 12 | trainer_name | str | 調教師名 | `友道康夫` |
| 60 | 3 | trainer_idx | int | 調教師指数（総合評価） | `050` |
| 63 | 3 | stable_idx | int | 厩舎指数（仕上げ力） | `052` |
| 66 | 3 | training_idx | int | 調教指数（直近の追い切り評価） | `046` |
| 69 | 1 | training_course_type | str | 調教コース種別 | `1`=坂路, `2`=ウッド, `3`=ダート, `4`=芝, `5`=プール |
| 70 | 1 | training_intensity | str | 調教強度 | `1`=強め, `2`=一杯, `3`=馬なり |
| 71 | 4 | training_time_4f | float | 4F調教タイム（秒、0.1単位） | `0523` = 52.3秒 |
| 75 | 4 | training_time_3f | float | 3F調教タイム | `0390` = 39.0秒 |
| 79 | 4 | training_time_1f | float | ラスト1F | `0124` = 12.4秒 |
| 83 | 250 | paddock_comment | str | パドックコメント（全角） | `好気配。踏み込み良。` |
| 333 | 250 | expert_comment | str | 記者コメント | `調教良化。前走不利あり。` |

#### 指数の意味

- **騎手指数**: 過去の勝率・連対率を加味した総合評価（高いほど優秀）
- **調教師指数**: 厩舎の総合力（ローテーション管理、使い分け等）
- **厩舎指数**: 直近の仕上げ状態（馬体重管理、調子の見極め）
- **調教指数**: 追い切りのタイムと動きから算出される「仕上がり度」

---

### 3. BAC（馬場データ）

**ファイル名**: `BACyymmdd.txt`  
**レコード長**: 約300バイト  
**1レコード**: 1レース分の馬場情報

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー | `24123105` |
| 8 | 3 | track_bias_idx | int | 馬場差指数 | `105` = +5（高速馬場） |
| 11 | 1 | track_speed_class | str | 馬場の速さクラス | `A`=最高速, `B`=高速, `C`=標準, `D`=タフ, `E`=超タフ |
| 12 | 3 | straight_bias | int | 直線伸び指数 | `098` = -2（やや伸びない） |
| 15 | 1 | inside_outside_bias | str | 内外差 | `1`=内有利, `2`=外有利, `3`=フラット |
| 16 | 1 | pace_bias | str | ペース傾向 | `1`=スロー, `2`=ミドル, `3`=ハイ |
| 17 | 3 | winning_speed_idx | int | 勝ちタイム指数（その日の優勝馬のIDM） | `068` |

**注意**: BACデータは開催日ごとに異なり、馬場改修やメンテナンスの影響を受ける。時系列でトレンドを追うと、高速化・重馬場化の傾向が見える。

---

### 4. CYB（前日情報）

**ファイル名**: `CYByymmdd.txt`  
**レコード長**: 約600バイト  
**1レコード**: 1頭の前日時点での情報

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー | `26021405` = 2026年2月14日東京5R |
| 8 | 2 | horse_no | int | 馬番 | `05` |
| 10 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 20 | 4 | prev_idm | int | 前日IDM | `0065` = 65 |
| 24 | 1 | jrdb_mark | str | JRDB印 | `◎` `○` `▲` `△` `×` |
| 25 | 3 | prev_pace_idx | int | 前日ペース指数 | `040` |
| 28 | 3 | prev_training_idx | int | 前日調教指数 | `045` |
| 31 | 1 | paddock_evaluation | str | パドック評価 | `A`=最良, `B`=良, `C`=普通, `D`=悪 |
| 32 | 6 | prev_odds_win | float | 前日単勝オッズ（0.1倍単位） | `000023` = 2.3倍 |
| 38 | 2 | prev_popularity | int | 前日人気順 | `01` |
| 40 | 250 | prev_comment | str | 前日コメント | `調教良好。本命視。` |

**重要性**: 
- 前日IDMは、レース当日の最新状態を反映する前の「ベースライン能力」として使用
- 前日オッズと確定オッズの差分（オッズドリフト）は、インサイダー情報の検出に有効

---

### 5. CHA（調教データ）

**ファイル名**: `CHAyymmdd.txt`  
**レコード長**: 約400バイト  
**1レコード**: 1頭の直近調教情報

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 10 | 8 | training_date | str | 調教日 | `20260210` |
| 18 | 1 | training_course | str | 調教コース | `1`=坂路, `2`=ウッド, `3`=ダート, `4`=芝, `5`=プール |
| 19 | 1 | training_intensity | str | 強度 | `1`=強め, `2`=一杯, `3`=馬なり |
| 20 | 4 | time_4f | float | 4F（0.1秒単位） | `0523` = 52.3秒 |
| 24 | 4 | time_3f | float | 3F | `0390` = 39.0秒 |
| 28 | 4 | time_last_1f | float | ラスト1F | `0124` = 12.4秒 |
| 32 | 1 | assistant_type | str | 助手種別 | `1`=男性助手, `2`=女性助手, `3`=騎手本人 |
| 33 | 250 | training_comment | str | 調教所見 | `時計良好。末脚使える。` |

**活用法**:
- ラスト1Fが12秒台前半 → 「切れる脚」があり、差し・追込で好走期待
- 4F-52秒台以下 → 調教強度が高く、「仕上がり万全」のサイン

---

### 6. SKB（成績拡張データ）

**ファイル名**: `SKByymmdd.txt`  
**レコード長**: 約800バイト  
**1レコード**: 1頭の拡張情報（外厩、放牧等）

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー | `26021405` |
| 8 | 2 | horse_no | int | 馬番 | `05` |
| 10 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 20 | 30 | outside_stable_name | str | 外厩名 | `ノーザンファームしがらき` |
| 50 | 2 | outside_stable_days | int | 外厩日数 | `45` = 45日間 |
| 52 | 1 | outside_stable_return | str | 帰厩後日数 | `7` = 7日前に帰厩 |
| 53 | 30 | pasture_name | str | 放牧先 | `優駿ファーム` |
| 83 | 2 | pasture_days | int | 放牧日数 | `60` |
| 85 | 30 | rest_reason | str | 休養理由 | `屈腱炎` `疲労` `放牧` 等 |
| 115 | 2 | rest_months | int | 休養月数 | `06` = 6ヶ月休養 |

**AI活用ポイント**:
- 外厩データ: 特定の外厩（例: ノーザンファーム）からの復帰馬は好成績が多い
- 放牧帰り: 長期休養明けの馬は1-2走目は調整途上、3走目以降で本格化するパターンが多い

---

### 7. TYB（当日情報）

**ファイル名**: `TYByymmdd.txt`  
**レコード長**: 約500バイト  
**1レコード**: 1頭の当日パドック所見

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 8 | race_key | str | レース識別キー | `26021405` |
| 8 | 2 | horse_no | int | 馬番 | `05` |
| 10 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 20 | 1 | paddock_body_type | str | 馬体評価 | `A`=良, `B`=普通, `C`=悪 |
| 21 | 1 | paddock_mood | str | 気配 | `1`=好気配, `2`=普通, `3`=イレ込み, `4`=元気なし |
| 22 | 1 | coat_condition | str | 毛ヅヤ | `A`=良, `B`=普通, `C`=悪 |
| 23 | 250 | paddock_detail_comment | str | パドック詳細コメント | `馬体良好。気合十分。リラックス。` |

---

### 8. UKC（馬基本データ）

**ファイル名**: `UKC.txt`（日付なし、マスターファイル）  
**レコード長**: 約250バイト  
**1レコード**: 1頭のマスター情報

#### 主要カラム定義

| 開始位置 | バイト長 | カラム名 | データ型 | 説明 | 例 |
|---------|---------|---------|---------|------|-----|
| 0 | 10 | horse_id | str | JRDB馬ID | `2019001234` |
| 10 | 36 | horse_name | str | 馬名 | `ドウデュース` |
| 46 | 8 | birthday | str | 生年月日 | `20190401` |
| 54 | 1 | sex | str | 性別 | `1`=牡, `2`=牝, `3`=セン |
| 55 | 2 | hair_color | str | 毛色 | `01`=栗毛, `04`=黒鹿毛, `07`=芦毛 |
| 57 | 30 | breeder_name | str | 生産者 | `社台ファーム` |
| 87 | 10 | sire_id | str | 父馬ID | `2010050123` |
| 97 | 36 | sire_name | str | 父馬名 | `ハーツクライ` |
| 133 | 10 | dam_id | str | 母馬ID | `2012080456` |
| 143 | 36 | dam_name | str | 母馬名 | `ダンスオールナイト` |

---

## Python実装サンプル

### 基底パーサークラス

```python
import os
import lzma
import struct
from typing import Dict, Any, List, Tuple

class JRDBRecordParser:
    """
    JRDB固定長レコードの基底パーサー
    """
    def __init__(self, encoding='cp932'):
        self.encoding = encoding
        self.specs: List[Tuple[int, int, str, type, Any]] = []
        # (開始位置, バイト長, カラム名, データ型, スケール係数)
    
    def parse_line(self, line_bytes: bytes) -> Dict[str, Any]:
        """
        1行のバイト列を辞書に変換
        """
        record = {}
        for start, length, name, dtype, scale in self.specs:
            chunk = line_bytes[start:start + length]
            
            try:
                text = chunk.decode(self.encoding).strip()
            except UnicodeDecodeError:
                text = chunk.decode(self.encoding, errors='replace').strip()
            
            # 欠損値の判定
            if text == '' or text == ' ' * length or text == '0' * length:
                record[name] = None
            else:
                try:
                    if dtype == int:
                        record[name] = int(text)
                    elif dtype == float:
                        record[name] = float(text) * scale if scale else float(text)
                    else:
                        record[name] = text
                except (ValueError, TypeError):
                    record[name] = None
        
        return record
    
    def parse_file(self, file_path: str) -> List[Dict[str, Any]]:
        """
        ファイル全体をパースしてレコードリストを返す
        """
        records = []
        with open(file_path, 'rb') as f:
            for line in f:
                line = line.rstrip(b'\r\n')
                if not line:
                    continue
                records.append(self.parse_line(line))
        return records
```

### SED（成績データ）パーサー

```python
class SEDParser(JRDBRecordParser):
    def __init__(self):
        super().__init__()
        self.specs = [
            (0, 8, 'race_key', str, None),
            (8, 2, 'horse_no', int, None),
            (10, 10, 'horse_id', str, None),
            (20, 4, 'idm', int, None),
            (24, 3, 'pace_idx', int, None),
            (27, 3, 'track_idx', int, None),
            (30, 3, 'up_idx', int, None),
            (33, 3, 'position_idx', int, None),
            (36, 3, 'tenkai_idx', int, None),
            (39, 1, 'tenkai_mark', str, None),
            (40, 2, 'distance_suitability', int, None),
            (42, 2, 'surface_suitability', int, None),
            (44, 2, 'class_code', int, None),
            (46, 2, 'finish_order', int, None),
            (48, 4, 'finish_time_sec', float, 0.1),
            (52, 3, 'last_3f_time', float, 0.1),
            (55, 2, 'corner1_position', int, None),
            (57, 2, 'corner2_position', int, None),
            (59, 2, 'corner3_position', int, None),
            (61, 2, 'corner4_position', int, None),
            (63, 4, 'horse_weight', int, None),
            (67, 3, 'horse_weight_diff', int, None),
            (70, 3, 'jockey_idx', int, None),
            (73, 3, 'trainer_idx', int, None),
            (76, 3, 'stable_idx', int, None),
        ]

# 使用例
parser = SEDParser()
records = parser.parse_file('data/jrdb/raw/SED241231.txt')
print(f"Parsed {len(records)} records")
print(records[0])
```

### KYI（騎手・調教師データ）パーサー

```python
class KYIParser(JRDBRecordParser):
    def __init__(self):
        super().__init__()
        self.specs = [
            (0, 8, 'race_key', str, None),
            (8, 2, 'horse_no', int, None),
            (10, 10, 'horse_id', str, None),
            (20, 5, 'jockey_code', str, None),
            (25, 12, 'jockey_name', str, None),
            (37, 3, 'jockey_idx', int, None),
            (40, 3, 'jockey_expectancy_idx', int, None),
            (43, 5, 'trainer_code', str, None),
            (48, 12, 'trainer_name', str, None),
            (60, 3, 'trainer_idx', int, None),
            (63, 3, 'stable_idx', int, None),
            (66, 3, 'training_idx', int, None),
            (69, 1, 'training_course_type', str, None),
            (70, 1, 'training_intensity', str, None),
            (71, 4, 'training_time_4f', float, 0.1),
            (75, 4, 'training_time_3f', float, 0.1),
            (79, 4, 'training_time_1f', float, 0.1),
            (83, 250, 'paddock_comment', str, None),
            (333, 250, 'expert_comment', str, None),
        ]
```

### LZHファイル解凍ユーティリティ

```python
import subprocess
import os

def extract_lzh_file(lzh_path: str, output_dir: str):
    """
    LZHファイルを解凍（7-zipを使用）
    """
    if not os.path.exists(output_dir):
        os.makedirs(output_dir)
    
    # 7-zipコマンド（Windows環境）
    cmd = f'7z x "{lzh_path}" -o"{output_dir}" -y'
    
    result = subprocess.run(cmd, shell=True, capture_output=True, text=True)
    
    if result.returncode == 0:
        print(f"Extracted: {lzh_path} -> {output_dir}")
    else:
        print(f"Error extracting {lzh_path}: {result.stderr}")

# 使用例
extract_lzh_file('E:/jrdb_data/raw/SED241231.lzh', 'E:/jrdb_data/extracted/')
```

---

## JRA-VANとの統合戦略

### 結合キーの設計

JRDBとJRA-VANのデータは、以下の複合キーで結合可能：

```python
# JRDBのrace_key: '26021405' = 2026年2月14日 東京(05場) 5R
# 分解:
year = race_key[0:2]  # '26' → '2026'
month = race_key[2:4]  # '02'
day = race_key[4:6]  # '14'
place_code = race_key[6:7]  # '0' (JRDBは1桁)
race_no = race_key[7:8]  # '5'

# JRA-VANのキー構成:
# race_date: '20260214'
# place_code: '05' (JRA-VANは2桁)
# race_no: '05' (JRA-VANは2桁ゼロ埋め)

# 統合キー生成:
def create_unified_key(jrdb_race_key: str) -> str:
    year = '20' + jrdb_race_key[0:2]
    month = jrdb_race_key[2:4]
    day = jrdb_race_key[4:6]
    place = jrdb_race_key[6:7].zfill(2)  # 1桁→2桁
    race = jrdb_race_key[7:8].zfill(2)   # 1桁→2桁
    return f"{year}{month}{day}_{place}_{race}"

# 例: '26021405' → '20260214_05_05'
```

### 場所コード変換マッピング

```python
# JRDBは1桁、JRA-VANは2桁
PLACE_CODE_MAP = {
    '1': '01',  # 札幌
    '2': '02',  # 函館
    '3': '03',  # 福島
    '4': '04',  # 新潟
    '5': '05',  # 東京
    '6': '06',  # 中山
    '7': '07',  # 中京
    '8': '08',  # 京都
    '9': '09',  # 阪神
    'A': '10',  # 小倉
}
```

### 統合テーブルの例（SQL）

```sql
CREATE TABLE unified_race_results (
    unified_key VARCHAR(20) PRIMARY KEY,  -- '20260214_05_05'
    race_date DATE,
    place_code VARCHAR(2),
    race_no INT,
    horse_no INT,
    
    -- JRA-VAN由来
    jv_bloodline_id VARCHAR(10),
    jv_horse_name VARCHAR(36),
    jv_finish_order INT,
    jv_finish_time_sec FLOAT,
    jv_odds_win FLOAT,
    
    -- JRDB由来
    jrdb_horse_id VARCHAR(10),
    jrdb_idm INT,
    jrdb_pace_idx INT,
    jrdb_training_idx INT,
    jrdb_paddock_comment TEXT,
    
    -- メタデータ
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 統合処理のPythonコード例

```python
import pandas as pd
from sqlalchemy import create_engine

def merge_jravan_jrdb(jv_df: pd.DataFrame, jrdb_df: pd.DataFrame) -> pd.DataFrame:
    """
    JRA-VANとJRDBのデータフレームを結合
    """
    # JRDBの統合キー生成
    jrdb_df['unified_key'] = jrdb_df['race_key'].apply(create_unified_key)
    
    # JRA-VANの統合キー生成
    jv_df['unified_key'] = (
        jv_df['race_date'] + '_' +
        jv_df['place_code'] + '_' +
        jv_df['race_no'].str.zfill(2)
    )
    
    # 馬番でも結合
    jv_df['horse_no'] = jv_df['horse_no'].astype(int)
    jrdb_df['horse_no'] = jrdb_df['horse_no'].astype(int)
    
    # LEFT JOIN（JRA-VANを基準）
    merged_df = pd.merge(
        jv_df,
        jrdb_df,
        on=['unified_key', 'horse_no'],
        how='left',
        suffixes=('_jv', '_jrdb')
    )
    
    return merged_df

# 使用例
engine = create_engine('sqlite:///jra_integrated.db')

jv_df = pd.read_sql('SELECT * FROM jravan_results', engine)
jrdb_df = pd.read_sql('SELECT * FROM jrdb_sed', engine)

merged = merge_jravan_jrdb(jv_df, jrdb_df)
merged.to_sql('unified_race_results', engine, if_exists='append', index=False)
```

---

## データ統計（15年分の見積もり）

| 項目 | 推定値 |
|------|--------|
| 総レース数 | 約30,000レース（年間2,000 × 15年） |
| 総出走頭数 | 約450,000頭（レースあたり平均15頭） |
| SEDファイル数 | 約5,500ファイル（年間365日 × 15年） |
| KYIファイル数 | 約5,500ファイル |
| LZH圧縮サイズ | 10-20GB |
| 解凍後サイズ | 30-50GB |
| DB格納後 | 40-70GB |

---

## 参考資料

- [JRDB公式サイト](http://www.jrdb.com/)
- JRDB会員専用ページ（データ仕様書DL可能）
- DBmanツール（JRDB提供のデータ閲覧ソフト）

---

**注意事項**:
- 上記バイト位置は一般的な定義であり、JRDBのバージョンにより変動する可能性があります。
- 必ずJRDB公式仕様書（会員ページからDL）を参照し、実データで検証してください。
- LZH解凍には7-zipまたはLhaplusを使用してください。
- エンコーディングはShift-JIS (cp932) 固定です。

---

**更新履歴**:
- 2026-02-14: 初版作成（Phase 0 データ取得用）
