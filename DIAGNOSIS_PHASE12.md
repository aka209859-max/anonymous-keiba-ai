# Phase 12 馬単予測システム - 問題診断レポート

## 🎯 現状の問題

**出力結果で人気薄が多く指名される**

### 具体例（浦和 2026-04-23）

| レース | 1位候補 | 1着確率 | 判定 |
|--------|---------|---------|------|
| Race 1 | 9番 | 38.3% | 本命級 ✅ |
| Race 2 | 5番 | 33.5% | 本命級 ✅ |
| Race 3 | 3番 | 30.1% | 微妙 ⚠️ |
| Race 4 | 7番 | **26.4%** | **人気薄 ❌** |
| Race 5 | 2番 | 45.8% | 本命級 ✅ |

**→ 1着確率30%未満の馬が推奨されている**

---

## 🔬 原因の仮説

### 仮説1: トレーニングデータの偏り

#### 🧪 検証項目

1. **データ分布の不均衡**
   - 人気薄が2着以内に入ったレアケースを過学習
   - クラスバランスが悪い（2着以内 ≈ 15-20%）

2. **特徴量の問題**
   - オッズ情報が欠落 → 人気度を判断できない
   - 過去成績のみで判断 → 実力評価に偏る

3. **評価指標のミスマッチ**
   - Top-K Accuracy（上位K頭に入れば正解）で評価
   - → 穴馬を含めても評価が高くなる

#### ❌ データに含まれていない情報

| 情報 | Phase 12 | 影響 |
|------|----------|------|
| **オッズ** | ❌ なし | 人気度判断不可 |
| **馬名** | ❌ なし | 視認性のみ |
| **当日人気順** | ❌ 不可能 | レース前情報なし |
| **調教タイム** | ❌ なし | 調子判断不可 |

---

### 仮説2: アンサンブル重みの問題

現在の重み設定：

```python
ensemble_score = 
    0.6 * binary_norm    # 2着以内確率
  + 0.3 * ranking_norm   # ランキングスコア
  + 0.1 * time_norm      # タイム予測
```

#### ⚠️ 問題点

- **binary（2着以内）の重みが60%** → 3着以下でも高スコアになりやすい
- **ranking の重みが低い（30%）** → 1着優先度が弱い
- **time の重みが極小（10%）** → タイム予測がほぼ無視される

---

### 仮説3: フィルタリング閾値が緩い

現在の設定：

```python
min_binary_proba = 0.25  # 25%
```

**→ 2着以内確率25%以上の馬を全て候補にしている**

#### 🎲 確率の意味

| 2着以内確率 | 意味 | 判定 |
|-------------|------|------|
| 50% 以上 | 本命級 | ✅ 推奨 |
| 35-50% | 対抗～穴 | ⚠️ 条件付き |
| 25-35% | 大穴 | ❌ 危険 |
| 25% 未満 | 超大穴 | ❌ 除外 |

**→ 現在は25%以上を推奨 = 大穴を含む**

---

## 🎯 馬単的中率を最大化するための改善案

### ✅ 改善策1: フィルタリング閾値の引き上げ（実施済み）

```batch
REM 修正前
python step7_generate_umatan.py "%ENSEMBLE_OUT%" "%UMATAN_OUT%"
# min_binary_proba = 0.25 (25%)

REM 修正後 ✅
python step7_generate_umatan.py "%ENSEMBLE_OUT%" "%UMATAN_OUT%" 5 4 0.30
# min_binary_proba = 0.30 (30%)
```

**期待される効果：**
- 大穴（25-30%）を除外
- 本命～対抗に絞り込み

---

### ✅ 改善策2: アンサンブル重みの最適化

#### 🎯 提案A: 1着重視型

```python
ensemble_score = 
    0.4 * binary_norm     # 2着以内確率（下げる）
  + 0.5 * ranking_norm    # ランキングスコア（上げる）
  + 0.1 * time_norm       # タイム予測
```

#### 🎯 提案B: 極端な1着特化型

```python
ensemble_score = 
    0.3 * binary_norm     # 2着以内確率（さらに下げる）
  + 0.6 * ranking_norm    # ランキングスコア（最大化）
  + 0.1 * time_norm       # タイム予測
```

---

### ✅ 改善策3: Top-N 候補数の削減

現在の設定：

```python
top_n = 5  # 上位5頭を候補にする
max_combinations = 4  # 馬単4通り
```

#### 🎯 提案

```python
top_n = 4  # 上位4頭に絞る
max_combinations = 3  # 馬単3通りに絞る
```

**期待される効果：**
- 本命度の高い上位のみに絞る
- 買い目数を減らして回収率向上

---

### ✅ 改善策4: 確率閾値の動的調整

#### 現在の問題

- 全レース一律で25%（または30%）
- レースの頭数や展開を考慮していない

#### 🎯 提案: レース別動的閾値

```python
# レースごとに1位確率を見て閾値を調整
max_proba = df.groupby('race_id')['binary_proba'].max()

if max_proba > 0.5:
    # 本命がいる → 閾値を高く（本命重視）
    threshold = 0.35
elif max_proba > 0.4:
    # 中本命 → 標準閾値
    threshold = 0.30
else:
    # 混戦 → 閾値を下げる（上位を広く取る）
    threshold = 0.25
```

---

## 📊 改善効果の予測

| 改善策 | 効果 | 的中率への影響 |
|--------|------|----------------|
| **1. 閾値30%** | 大穴除外 | +5% 🔼 |
| **2. 重み最適化** | 1着重視 | +8% 🔼 |
| **3. Top-N削減** | 本命集中 | +3% 🔼 |
| **4. 動的閾値** | 柔軟対応 | +10% 🔼 |

**合計期待効果: 的中率 +15~25%**

---

## 🚀 次のアクション

### E:ドライブで実行すべきコマンド

```powershell
cd E:\anonymous-keiba-ai

# 1. 最新版を取得
git fetch origin phase0_complete_fix_2026_02_07
git reset --hard origin/phase0_complete_fix_2026_02_07

# 2. 修正済みバッチで再予測（閾値30%）
run_phase12_umatan.bat 42 2026-04-23

# 3. 結果確認
type predictions\phase12_umatan\浦和_20260423_umatan.txt

# 4. 元の結果と比較
# Before: 閾値25% → 人気薄多い
# After:  閾値30% → 本命寄り
```

### さらなる改善を試す場合

```powershell
# アンサンブル重みを変更してテスト
python scripts\phase12_umatan_model\step6_ensemble.py ^
  data\phase12_umatan\predictions\binary\浦和_20260423_binary.csv ^
  data\phase12_umatan\predictions\ranking\浦和_20260423_ranking.csv ^
  data\phase12_umatan\predictions\regression\浦和_20260423_regression.csv ^
  data\phase12_umatan\predictions\ensemble\浦和_20260423_ensemble.csv ^
  0.4 0.5 0.1  # 重み: binary, ranking, regression

# 馬単生成（閾値35%、上位4頭、3通り）
python scripts\phase12_umatan_model\step7_generate_umatan.py ^
  data\phase12_umatan\predictions\ensemble\浦和_20260423_ensemble.csv ^
  predictions\phase12_umatan\浦和_20260423_umatan_strict.txt ^
  4 3 0.35
```

---

## 📝 まとめ

### トレーニングデータ

- **データ**: 過去の全レース（競馬場別CSV）
- **特徴量**: 約50列（基本 + 2着特化）
- **ターゲット**: is_top2（2着以内）
- **問題点**: オッズ情報なし、人気度判断不可

### 人気薄が多い原因

1. **トレーニングデータに人気情報がない**
2. **アンサンブル重みが2着以内に偏っている**（0.6）
3. **フィルタリング閾値が緩い**（25%）
4. **Top-K評価で穴馬も許容される**

### 対策（優先順位）

1. ✅ **閾値を30%に引き上げ**（実施済み）
2. 🔜 **アンサンブル重みを1着重視に変更**（0.4/0.5/0.1）
3. 🔜 **Top-N候補数を削減**（5→4頭、4→3通り）
4. 🔜 **動的閾値の実装**（レース展開に応じた調整）

---

**妥協なし、馬単的中を最優先！** 🏇💰
